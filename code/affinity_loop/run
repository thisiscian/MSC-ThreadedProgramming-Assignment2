#/bin/bash
input=src/loops2.c
program=bin/loops2
num_threads=2

./compile $input $output
if [ -a $output ]; then
	export OMP_NUM_THREADS=$num_threads
	./$program
else
	echo "File: '$output' not found, exitting..."
fi
